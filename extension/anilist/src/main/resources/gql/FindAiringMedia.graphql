query ($page: Int, $perPage: Int, $mediaIn: String, $episodeGreaterThan: Int) {
  Page(page: $page, perPage: $perPage) {
    airingSchedules(mediaId_in: $mediaIn, episode_greater: $episodeGreaterThan) {
      id,
      episode,
      mediaId,
      media {
        title {
          english
          romaji
          native
        }
        coverImage {
          extraLarge
        }
        siteUrl
      }
    }
  }
}
